class Expenses {
  final String title;
  final String to;
  final String summary;
  final String amount;
  final String when;

  Expenses(this.title, this.to, this.summary, this.amount, this.when);
}
