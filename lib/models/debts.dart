import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:credit_card/models/expenses.dart';

class Debts {
  // extends Expenses {
  // final DateTime due;

  // Debts(title, to, contactInfo, this.due, summary, amount, when)
  //     : super(title, to, contactInfo, summary, amount, when);

  // factory Debts.fromJson(Map<String, dynamic> json) {
  //   return Debts(
  //       json['title'] as String,
  //       json['to'] as String,
  //       json['contactInfo'] as String,
  //       DateTime.fromMicrosecondsSinceEpoch(
  //           (json['due'] as Timestamp).microsecondsSinceEpoch),
  //       json['summary'] as String,
  //       json['amount'] as String,
  //       DateTime.fromMicrosecondsSinceEpoch(
  //           (json['when'] as Timestamp).microsecondsSinceEpoch));
  // }
}
